{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "serve",
      "program": "${workspaceFolder}/node_modules/.bin/vue-cli-service",
      "args": ["serve"],
      "env": {
        "VUE_CLI_SERVICE_CONFIG_PATH": "${workspaceFolder}/vue.config.js"
      }
    }
  ]
}
